import React from 'react'

export default function Header() {
  const handleShow = () => {
    // do something
  }

  return (
    <div>
      Header
      <button onClick={handleShow}>Show</button>
    </div>
  )
}
